# These are the Python packages this project needs to run.
# Install them by running: pip install -r requirements.txt

google-api-python-client==2.118.0   # Lets Python talk to YouTube's search API
python-dotenv==1.0.1                # Lets Python read your secret API keys from a .env file
anthropic==0.49.0                   # Lets Python talk to Claude (optional — only needed for smart phrase generation)
youtube-transcript-api==0.6.3       # Step 2: Downloads transcripts/captions from YouTube videos (no API key needed)
